---
import { langs } from '../utils/LanguageList'

export interface Props {
  techs: string[]
}

const { techs } = Astro.props
---

<ul class={'grid mt-4 grid-cols-3'}>
  {
    techs.map(arr => {
      let color = ''
      if (Object.keys(langs).includes(arr)) {
        color = langs[arr].color
      }
      return color ? (
        <li style={`background-color: ${color};`} class={`m-1 p-1 text-black rounded-lg flex justify-center items-center`}>
          <p class="text-sm text-center">{arr}</p>
        </li>
      ) : (
        <li class={`m-1 p-1 bg-[beige] text-black rounded-lg flex justify-center items-center`}>
          <p class="text-sm text-center">{arr}</p>
        </li>
      )
    })
  }
</ul>
